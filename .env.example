# Optional environment variables
# Copy to .env if you need them. Never commit real values.

# Enable verbose frontend perf logging to the Rust debug log
# VITE_OPAL_PERF=1

# DEV screenshot fixtures (balances/txs) are ON by default in vite/tauri:dev.
# Opt out: in the app console → localStorage.setItem("opal:demo", "0")
